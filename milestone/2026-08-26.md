# 2026-08-26 Work Log

## Strategy Shadow Governance

- Added a version-controlled strategy registry and an order-free governance
  evaluator. It accepts structured daily research snapshots and applies a
  fixed policy to parameters, OOS selection discipline, costs, data freshness,
  risk review, Walk-Forward windows, rolling OOS evidence, drawdown, turnover,
  and shadow-observation duration.
- The decision ledger is append-only and permits only one decision for each
  strategy/as-of-date pair. The evaluator can return only an advisory paper
  review result and hard-codes both paper and live authorization to false.
- Defensive ETF is the only current shadow-observation strategy. ARC, Stock
  Turtle, Range Grid, SPY Mean Reversion, and AHL-inspired Global Futures Trend
  remain research-only and cannot be promoted by a short-lived backtest lead.
- Added tests for fixed-gate failures, research-only blocking, no-order
  authorization, duplicate prevention, scheduler-safe duplicate skips, and
  Defensive ETF snapshot construction.
- Extended the Windows Defensive ETF observation wrapper to rerun the frozen
  backtest and Walk-Forward report, build a fresh governance snapshot, and
  append its advisory decision. The risk review is false by default, so the
  scheduled research path cannot promote the strategy or submit an order.
