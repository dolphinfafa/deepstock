# 2026-08-22 Work Log

## Norgate Platinum Entitlement Check

- Reconnected to the Windows Norgate node after the user purchased the
  Platinum subscription.
- `norgatedata.status()` remained healthy and the expected databases were
  visible, but `AAPL` still reported `first_quoted_date=2024-08-21` and
  returned 502 daily rows through 2026-08-21 when queried from 1990 onward.
- The result indicates that the local Norgate Data Updater has not yet
  refreshed the new entitlement, or that its logged-in account differs from
  the paid account. No bulk download was started.
- Next action: refresh/re-authenticate the Norgate Data Updater, run its data
  update, then repeat the AAPL and delisted-symbol history checks before any
  large download.

## Second Entitlement Check

- Repeated the query after the user requested another check. The package still
  initialized, but `AAPL` now returned empty first/last quote dates and
  `price_timeseries` failed with Norgate's explicit `Access is denied` response.
- This is an authorization/session problem rather than a short-history result.
  Bulk downloading remains stopped until NDU is logged into the paid account
  and shows the US Equities subscription as active.

## Platinum History Verification

- After the Norgate database-folder change and updater restart, Platinum
  history access was verified: `AAPL` returned 9,210 daily rows beginning
  1990-01-02. The paid entitlement is active.
- The local updater has not completed its data refresh. `AAPL` currently ends
  on 2026-07-29, `US Equities` lists 14,285 symbols, and `US Equities Delisted`
  is not yet available through the Python API. Do not start the staged download
  until the updater finishes and both current and delisted databases are fresh.

## Platinum Database Refresh Complete

- Confirmed the refresh is complete. `AAPL` now has 9,227 daily bars from
  1990-01-02 through 2026-08-21.
- `US Equities Delisted` is available with 21,134 symbols. A sampled delisted
  security (`A-199008`) returned 127 daily bars from 1990-01-02 to 1990-08-14.
- The Windows node is ready for a staged, resumable full-universe download;
  no broker or trading action was performed.

## Long-History ETF Robustness Baseline

- Downloaded the seven ETF symbols from Norgate to a local, Git-ignored
  research file: 45,596 rows, common complete-period start 2004-11-18 and end
  2026-08-21. The source uses Norgate total-return `Close` values.
- Re-ran the predeclared 32-combination grid with a fixed 2010-01-04 split.
  The earlier selected configuration was not reselected from this new result;
  this run is a robustness check only. Across the 2010-2026 out-of-sample
  segment, the grid's best result had Sharpe 1.01 and maximum drawdown -11.86%,
  versus SPY total return 820.70% (the strategy's return was 221.45%).
- Applied transaction-cost stress to the frozen 252/200, top-2, 80%/40%
  configuration. Full-period Sharpe was 1.02 at 0 bps, 1.00 at 5 bps, 0.98 at
  10 bps, and 0.94 at 20 bps; maximum drawdown ranged from -11.56% to -12.12%.
- A 504-day train / 252-day test / 252-day step walk-forward produced 19
  windows. Results remain regime-dependent, including a -9.81% 2021-2022 test
  window, so no parameter change or paper-order authorization is justified.

## Norgate Database Folder Preflight

- Inspected the Windows process list before changing the Norgate database
  folder. The only Norgate process is `dataupdater.norgate.exe` (PID 43020).
- No AmiBroker, MetaStock, Wealth-Lab, RealTest, Python/Jupyter, RStudio,
  Excel, or other charting/analysis process that would ordinarily read the
  Norgate database was running.
- `tws.exe` is running, but it uses IBKR's own data connection and does not
  access the Norgate database. It does not need to be closed for this folder
  change; the user will close NDU before applying it.

## Order-Free ETF Observation Workflow

- Added a Windows-only Norgate exporter for the frozen defensive ETF universe.
  It writes total-return prices plus a local provenance manifest; licensed raw
  data remains Git ignored.
- Added a deterministic defensive ETF plan generator and append-only JSONL
  observation recorder. Both are order-free and have no broker dependency.
- Generated the first review-only plan from Norgate data dated 2026-08-21 and
  recorded it successfully. The target was 20% GLD, 20% IWM, and 60% SHY.
- Added an eight-week observation protocol with frozen parameters, acceptance
  criteria, and duplicate/stale-data/kill-switch gates. No paper or live order
  submission is authorized.
- Added a Windows Task Scheduler wrapper that runs export, plan generation, and
  observation recording sequentially, stops on failure, and writes a local
  scheduler log. Scheduling is configured separately after confirming the NDU
  update window.
- Updated `Deepstock_Defensive_ETF_Observation` to `WakeToRun=True` and
  `StartWhenAvailable=True`. Verified both settings remotely; the task remains
  order-free and was not manually triggered.

## Stock Turtle Universe Preparation

- Added a resumable Norgate exporter for the `S&P 500 Current & Past` watchlist.
  It writes adjusted-price chunks and compresses daily index-membership flags
  into historical intervals, preserving point-in-time eligibility information.
- Added an interval-compression regression test. A 10-symbol Windows pilot is
  the next validation step; no stock-Turtle parameter has been changed.
- The pilot succeeded, and the full S&P 500 current-and-past export then
  completed: 1,301 symbols in 14 price chunks, approximately 105 MB after
  transfer to the server. The manifest records 261 symbols without usable
  price history; these were retained as failures rather than filled.
- No stock-Turtle backtest has been run on this export yet. The current engine
  requires complete matrices and must first become membership-aware before
  these data can be evaluated without survivorship bias.
