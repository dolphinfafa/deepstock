# 2026-08-31 Work Log

## Defensive ETF Observation Policy v2

- Replaced the unused 40-session / approximately eight-week shadow-observation
  threshold with a fixed v2 requirement: 30 completed trading-session records
  spanning at least 42 calendar days. Both conditions are required, so a short
  burst of daily records cannot qualify the strategy.
- Set the v2 policy effective date to 2026-08-31. Earlier records remain in the
  append-only observation ledger for audit but are excluded from v2 counts; the
  first new completed market-data record begins the new clock.
- Retained all strategy-quality gates, including fixed parameters, no OOS
  parameter selection, costs, data freshness, risk review, Walk-Forward,
  rolling OOS performance, drawdown, turnover, and manual approval. The change
  does not enable paper or live orders.
- Added coverage for rejecting pre-policy decisions and excluding old
  observations from the new policy count.

## IBKR Paper API Read-Only Revalidation

- Revalidated the study computer (`DESKTOP-S31222F`) after confirming its
  working tree is clean at commit `d47c993`.
- Confirmed TWS Paper is listening locally on port `7497`; the local API
  configuration remains `paper`, `127.0.0.1`, client ID `102`, and
  `IBKR_READ_ONLY=true`.
- Ran `scripts/ibkr_read_only_check.py --json` successfully. It returned seven
  account-summary rows, zero positions, zero open orders, and healthy market,
  historical-data, and security-definition service connections.
- This was a connectivity-only check: no market-data subscription change,
  order-generation code, paper order, or live order was created.

## Study Computer Change-Control Rule

- Established `DESKTOP-S31222F` as a pull-only deployment and verification
  node for the tracked project. Future changes must be made and verified on
  this server, committed and pushed to GitHub, then fast-forwarded on the study
  computer.
- The study computer may retain machine-local, untracked configuration and
  runtime data, but its tracked project files must not be edited directly.

## SGOV Paper Order Smoke Test

- Added a Paper-only API smoke test that can submit and then cancel exactly one
  deliberately non-marketable `SGOV` buy order (one share, `$1.00` limit).
- The test rejects live mode, read-only mode, every non-SGOV contract, all
  quantities except one, and execution without both explicit CLI confirmations.
- Its purpose is limited to validating Paper order submission, order-status
  callbacks, and cancellation. It is not connected to a strategy or live
  trading.
