# 2026-08-19

## IBKR Read-Only Probe

- Normalized the local untracked `.env` keys to `IBKR_CLIENT_ID=101` and
  `IBKR_READ_ONLY=true`.
- An initial probe attempt against `127.0.0.1:7497` returned IBKR error 502
  while TWS was not accepting API connections.
- After TWS API access became available, the probe completed successfully.
  Server time and account summary returned; positions and open orders were both
  empty; no probe errors were reported.

## Strategy Research Implementation

- Implemented a reproducible defensive ETF backtest for `SPY`, `QQQ`, `IWM`,
  `TLT`, `IEF`, `GLD`, and defensive asset `SHY`.
- Signals require positive 12-month momentum and a close above the 200-day
  moving average. Eligible assets receive inverse-volatility weights, subject
  to 80% total exposure and 20% single-position caps; residual weight goes to
  `SHY`.
- Targets are calculated at month end and shifted one trading day before
  returns are applied. The backtest includes configurable transaction costs.
- Added CSV data validation, a CLI that writes reproducible artifacts, and unit
  tests. The implementation does not connect to TWS or contain order code.

## Verification

- Installed the approved project dependencies into the server `deepstock` Conda
  environment: `ibapi` 9.81.1.post1, `numpy` 2.5.2, `pandas` 2.3.3, and
  `pytest` 8.4.2.
- Passed four unit tests covering safe-asset fallback, one-day signal delay,
  transaction costs, and missing-symbol rejection.
- Completed an end-to-end synthetic CSV run that wrote daily results, target
  weights, executed weights, and summary metrics. Synthetic results are only a
  functional test and are not investment performance evidence.

## Next Step

- Selected Massive as the primary historical research-data source. Implemented
  a local, read-only adjusted-daily downloader with a non-secret manifest.
- Passed six unit tests, including simulated Massive pagination and adjusted
  daily-bar handling. Static checks confirm the downloader has no IBKR, TWS, or
  order-submission dependency.
- Actual Massive API retrieval remains a laptop-local operation because the key
  stays only in its untracked `.env`. No paper-order work is authorized.

## Documentation and Synchronization

- User confirmed the Massive API key is configured locally and the project
  dependency installation and test suite pass on the laptop.
- Updated workflow documents to mark IBKR read-only validation, Massive research
  data selection, and the current research-validation phase as complete facts.
- Updated `todo.md` to remove completed provider-selection work and focus on
  the first real historical dataset and backtest review.

## Documentation Verification

- Passed the six-test suite and dependency consistency check in the server
  `deepstock` environment.
- Confirmed the workflow references and `todo.md` reflect the selected Massive
  source and research-validation phase.
- Confirmed `.env` remains ignored and the documentation diff has no whitespace
  errors.
