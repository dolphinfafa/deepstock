# 2026-08-20

## Laptop SSH Verification Preparation

- User provided a jump-host SSH route to the laptop execution node and requested
  a remote verification connection.
- The initial SSH attempt stopped at jump-host key verification. No laptop shell
  was opened and no command ran on the laptop.
- The server has no verified `known_hosts` entry for the configured jump host.
  Do not bypass host-key verification; obtain and verify the jump-host SSH host
  key fingerprint before establishing the first connection.
- User authorized use of a dedicated private key stored outside the repository.
  Its permissions were tightened from `664` to `600` so SSH can use it; its
  contents were not read, copied, or committed.
- A second connection attempt using that key again stopped at jump-host key
  verification. Private-key authentication cannot substitute for verifying the
  remote host identity.
- The jump-host ED25519 fingerprint was subsequently confirmed by the user and
  matched the key retrieved by the server. The verified key was added to local
  `known_hosts`.
- The specified private key successfully authenticated the jump-host route. The
  laptop presented an unverified ED25519 host fingerprint, so the connection
  stopped before laptop authentication and no remote command executed.

## Verification

- The user-supplied SSH command established a read-only connection to the
  laptop. The project checkout is clean on `main` and tracks `origin/main`.
- The laptop `deepstock` interpreter is Python 3.12.13.
- No process was listening on the configured local paper API port, and no TWS
  or IB Gateway process was running. The IBKR read-only probe was therefore not
  invoked and no account data was queried.

## Next Step

- Start TWS in paper mode on the laptop, then rerun the read-only probe to
  confirm the local API is available before downloading research data or
  performing the first historical backtest.
