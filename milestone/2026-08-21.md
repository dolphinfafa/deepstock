# 2026-08-21

## Remote TWS Read-Only Validation

- Connected to the user's laptop through the verified SSH jump route.
- Confirmed the laptop project checkout and Python 3.12.13 environment.
- Confirmed TWS is running and its paper API listener is available on port 7497.
- Ran `scripts/ibkr_read_only_check.py` using the laptop-local `.env` without
  printing credentials or account values and without submitting orders.
- Probe result: paper mode, server time returned, seven account-summary rows,
  zero positions, zero open orders, and normal IBKR market-data service status.

## Security Finding

- TWS continues to listen on `*:7497`; access must remain restricted through
  TWS trusted-IP settings and no API port may be exposed or forwarded.

## Data Correction

- Verified Massive documentation: aggregate `adjusted=true` adjusts only for
  splits, not cash dividends.
- Replaced the research-data assumption with a sequential total-return
  construction that adds Massive split-adjusted cash dividends on ex-dividend
  dates. This avoids applying future corporate actions to past signals. The
  first real backtest will wait for this corrected downloader and its tests.

## Next Step

- Correct the TWS API listener restriction, rerun the read-only probe, then
  download Massive adjusted daily data and begin the first real backtest.
