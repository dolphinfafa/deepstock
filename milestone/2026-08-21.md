# 2026-08-21

## Remote TWS Read-Only Validation

- Connected to the user's laptop through the verified SSH jump route.
- Confirmed the laptop project checkout and Python 3.12.13 environment.
- Confirmed TWS is running and its paper API listener is available on port 7497.
- Ran `scripts/ibkr_read_only_check.py` using the laptop-local `.env` without
  printing credentials or account values and without submitting orders.
- Probe result: paper mode, server time returned, seven account-summary rows,
  zero positions, zero open orders, and normal IBKR market-data service status.

## Security Finding

- TWS continues to listen on `*:7497`; access must remain restricted through
  TWS trusted-IP settings and no API port may be exposed or forwarded.

## Data Correction

- Verified Massive documentation: aggregate `adjusted=true` adjusts only for
  splits, not cash dividends.
- Replaced the research-data assumption with a sequential total-return
  construction that adds Massive split-adjusted cash dividends on ex-dividend
  dates. This avoids applying future corporate actions to past signals. The
  first real backtest will wait for this corrected downloader and its tests.
- Diagnosed the first Massive import failure: all seven ETF aggregate requests
  returned valid `DELAYED` status under the user's subscription. Updated the
  downloader to accept that status only for historical research data.
- Downloaded a complete seven-ETF research dataset: 8,778 rows across 1,254
  aligned sessions. The subscription returned 2021-08-23 through 2026-08-20,
  despite a 2010 request; this five-year limit must be explicit in the manifest
  and in any performance interpretation.

## Initial Historical Backtest

- Ran the defensive ETF backtest against the local Massive total-return dataset
  for 2021-08-23 through 2026-08-20. No broker API or order capability was used.
- Strategy: total return 34.63%, annualized return 6.16%, annualized volatility
  6.24%, Sharpe ratio 0.99, maximum drawdown -6.25%, turnover 6.73, and modeled
  transaction costs 0.67% of initial capital.
- `SPY` benchmark total return over the same interval was 82.47%. The strategy
  reduced drawdown but materially underperformed the equity benchmark.
- This is an initial in-sample result on a subscription-limited five-year
  window. It does not authorize paper orders or establish expected performance.

## Next Step

- Review the fixed-split robustness result and add a regime comparison before
  considering any paper-order capability.

## Fixed-Split Robustness Validation

- Added a segmented backtest path. The split date is the first out-of-sample
  trading day; the out-of-sample segment is rebased for reporting but retains
  only weights generated causally from available prior history.
- Added `scripts/run_defensive_etf_robustness.py`. It records the exact input
  coverage, split, configuration grid, in-sample and out-of-sample metrics, and
  an explicit rule that ranks only by in-sample Sharpe ratio.
- On the laptop's local Massive total-return dataset, ran all 18 combinations:
  150/200/250-day trend, 126/252-day momentum, and 42/63/126-day volatility.
  The split was fixed at 2024-08-21: 753 in-sample sessions from 2021-08-23 to
  2024-08-20 and 501 out-of-sample sessions through 2026-08-20.
- Out-of-sample strategy returns ranged from 14.14% to 19.22%; annualized
  volatility ranged from 6.70% to 7.48%, Sharpe from 0.96 to 1.35, maximum
  drawdown from -5.12% to -4.42%, turnover from 2.63 to 4.45, and modeled costs
  from 0.26% to 0.44% of initial capital. `SPY` returned 39.72% in the same
  out-of-sample period.
- The highest in-sample-ranked configuration was 252-day momentum, 250-day
  trend, and 42-day volatility. Its out-of-sample total return was 19.22%,
  Sharpe 1.35, and maximum drawdown -4.55%. No parameter was selected using
  out-of-sample results.
- Conclusion: the narrow grid supports a stable defensive, low-drawdown
  profile, but does not support paper-order implementation because every tested
  configuration materially underperformed `SPY` over the out-of-sample period.

## Turtle Strategy Prototype

- Added `src/deepstock/turtle.py` and `scripts/run_turtle_backtest.py` as a
  separate research path. It is long-only, uses close-only channel breakouts,
  a close-based ATR proxy for position sizing, 2-ATR risk stops, and delays
  every target by one trading session.
- On 2021-08-23 through 2026-08-20, 55/20 returned 20.10%, annualized 3.75%,
  Sharpe 0.59, maximum drawdown -14.46%, turnover 27.87, and modeled costs
  2.79%. The faster 20/10 returned 19.79%, annualized 3.70%, Sharpe 0.55,
  drawdown -13.39%, turnover 64.38, and costs 6.44%. `SPY` returned 82.47%.
- These full-sample results show the prototype currently adds turnover and
  drawdown without improving returns. It remains research-only; fixed-split
  and walk-forward validation are required before paper-order work.

## Stock Turtle Variant

- Extended the Turtle engine to accept a caller-supplied stock universe and a
  `max_positions` cap. When more stocks break out than the cap, the engine keeps
  the strongest current breakout ratios. The ETF defaults remain available for
  backwards-compatible research runs.
- Added stock-input requirements to `data/README.md`: split/dividend-adjusted
  history, point-in-time universe construction, delisted names, liquidity
  filter and license manifest. A safe asset column is still required.
- Added validation for next-session execution and the stock position cap. No
  stock data was downloaded in this step, so no stock performance conclusion is
  being claimed and no order capability was added.

## Server Stock Data and Backtest

- Configured the server-only `.env` with the user-provided Massive key. The key
  remains untracked and was never printed or committed.
- Downloaded 18 symbols (`AAPL`, `MSFT`, `NVDA`, `AMZN`, `META`, `GOOGL`,
  `AVGO`, `TSLA`, `JPM`, `XOM`, `UNH`, `COST`, `WMT`, `HD`, `PG`, `KO`, `SHY`,
  `SPY`): 22,482 rows, 736 KB CSV, actual coverage 2021-08-23 through
  2026-08-20. The requested 2015 start was unavailable under the subscription.
- `META` had 90 missing sessions because its historical ticker was `FB` before
  2022. The backtest rejected incomplete data and excluded `META` rather than
  silently filling or mapping it.
- Fixed the backtest configuration to allow `SPY` as a separate benchmark that
  is present in the price matrix but never receives a target weight.
- On the remaining 15 stocks, 55/20 returned 64.32%, annualized 10.49%, Sharpe
  0.84, maximum drawdown -15.49%, turnover 89.36, and modeled costs 8.94%.
  20/10 returned 101.33%, annualized 15.10%, Sharpe 1.12, drawdown -12.39%,
  turnover 167.47, and costs 16.75%. `SPY` returned 82.47%.
- These are full-sample, survivor-prone exploratory results. The faster setup's
  apparent outperformance must not be selected or paper-traded before fixed
  split, ticker-mapping, delisting, liquidity, and cost robustness tests.

## Stock Turtle Fixed-Split Validation

- Added `scripts/run_turtle_robustness.py` and causal segment summaries. The
  split is fixed at 2024-08-21: 753 in-sample sessions and 501 out-of-sample
  sessions after complete-row filtering.
- Evaluated six predeclared combinations of entry `20/55`, exit `10/20`, and
  maximum positions `3/5`; ranking used only in-sample Sharpe and drawdown.
- The in-sample-ranked winner was 20/10 with five positions. Out of sample it
  returned 20.60%, annualized 9.88%, Sharpe 0.77, maximum drawdown -11.46%,
  turnover 62.51, and modeled costs 6.25%. `SPY` returned 39.72%.
- All six combinations underperformed `SPY`. The previous full-sample 20/10
  outperformance was not robust to the fixed split; no paper-order capability
  is authorized.

## Historical Ticker Mapping Validation

- Downloaded `FB` for 2021-08-23 through 2022-06-08 and merged it into the
  canonical `META` series beginning 2022-06-09. The merged dataset contains
  22,572 rows across 18 symbols and 1,254 complete sessions with zero missing
  values.
- Re-ran the six fixed-split combinations on the corrected 16-stock trade
  universe. The in-sample-ranked 20/10 with five positions returned 20.30% out
  of sample, annualized 9.74%, Sharpe 0.76, maximum drawdown -11.02%, turnover
  68.06, and modeled costs 6.81%; `SPY` returned 39.72%.
- All six combinations still underperformed `SPY`. The ticker correction did
  not alter the no-paper-orders conclusion. The universe is still today's
  selected names, so survivorship-free and liquidity-controlled research is
  required next.

## Sector-Cap Baseline

- Added optional static sector mapping and `max_per_sector` enforcement to the
  Turtle engine and robustness script. Symbols without a mapping remain their
  own sector, and the cap is disabled by default.
- Tested a snapshot mapping with at most two positions per sector across the
  corrected 16-stock universe. The in-sample-ranked 20/10 with five positions
  returned 5.90% out of sample, annualized 2.92%, Sharpe 0.29, maximum drawdown
  -12.67%, turnover 72.62, and modeled costs 7.26%; `SPY` returned 39.72%.
- The sector cap reduced concentration but materially reduced returns. The
  labels are not point-in-time classifications, so this is only a risk-control
  baseline and does not authorize paper orders.

## Adaptive Defensive ETF Validation

- Extended `StrategyConfig` with optional relative-momentum top-K selection,
  benchmark trend filtering, and separate above/below-filter exposure. Existing
  defaults preserve the original strategy behavior.
- Added `scripts/run_adaptive_defensive_grid.py`. It evaluates 32 predeclared
  combinations and ranks only by in-sample Sharpe and drawdown.
- On the ETF dataset (2021-08-23 through 2026-08-20), the fixed split at
  2024-08-21 ranked 252-day momentum, 200-day trend, top 2 assets, and 80%/40%
  exposure first in sample. Out of sample it returned 27.17%, annualized
  12.85%, Sharpe 1.87, maximum drawdown -5.79%, turnover 2.06, and modeled
  costs 0.21%; `SPY` returned 39.72%.
- The adaptive version materially improved on the original defensive result's
  14% to 19% out-of-sample range, but still underperformed `SPY`. Walk-forward
  and regime validation remain required; no paper-order capability is enabled.

## Adaptive ETF Rolling Validation

- Added `scripts/run_adaptive_defensive_walkforward.py` with a fixed
  504-session training history and configurable rolling test windows. The
  configuration is not reselected in any test window.
- The default 252-session test produced two complete windows, both below `SPY`.
  A 126-session test/step produced five windows covering 2023-08-24 through
  2026-02-27. Strategy maximum drawdown ranged from -3.30% to -1.36%; it
  underperformed `SPY` in the first four windows and outperformed only in the
  final window (12.38% versus 6.69%).
- Conclusion: the adaptive strategy has a consistently defensive drawdown
  profile and appears regime-dependent. It does not demonstrate persistent
  benchmark outperformance, so paper orders remain blocked pending an explicit
  risk/return acceptance decision.

## Paper Workflow Preparation

- Added `src/deepstock/paper_plan.py` and `scripts/generate_paper_plan.py`.
  The server-side output is deterministic (`plan_id`), marked `mode=paper`,
  and supports a kill switch. It generates target weights only; it has no IBKR
  connection and cannot submit orders.
- Re-ran the laptop TWS paper read-only probe through the existing SSH route.
  The connection returned server time, seven account-summary rows, zero
  positions, zero open orders, and no errors. Sensitive account values and
  identifiers are now redacted from probe output.
- Research tests passed (`15 passed`) and the new scripts compiled. The
  eight-week paper observation period has not elapsed; it must be started and
  logged on the laptop. Disconnect, restart, duplicate-run, rejected-order,
  and kill-switch tests remain gates before paper order capability.
